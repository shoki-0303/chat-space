.sidebar
  .sidebar__top
    %h3.sidebar__top__name
      = current_user.name
    .sidebar__top__icons
      = link_to new_group_path do
        %i.fa.fa-pencil-square-o
      = link_to edit_user_path(current_user) do
        %i.fa.fa-cog
  .sidebar__buttom
    = render "shared/group"
