.sidebar__buttom__group
  - current_user.groups.each do |group|
    = link_to group_messages_path(group) do
      %p.sidebar__buttom__group__name
        = group.name
      %p.sidebar__buttom__group__text
        メッセージはまだありません。


